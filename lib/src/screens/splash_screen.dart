import 'package:flutter/material.dart';

class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        // TODO: add spinner or some shit
        child: Text('MIT FLOTTE SPLASHSCREEN'),
      ),
    );
  }
}
